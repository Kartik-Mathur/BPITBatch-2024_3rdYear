#include <iostream>
using namespace std;

int main() {
	int a[1000];
	int n;

	cin >> n; // Because to take n inputs, we are asking user to provide n
	// values

	// Taking input of the array
	for (int i = 0; i < n; ++i)
	{
		cin >> a[i];
	}




}